# Google OAuth Configuration
# 
# To set up Google OAuth authentication:
# 1. Go to Google Cloud Console (https://console.cloud.google.com/)
# 2. Create a new project or select existing project
# 3. Enable Google+ API and Google Identity Services
# 4. Go to "Credentials" → "Create Credentials" → "OAuth 2.0 Client ID"
# 5. Set Application type to "Web application"
# 6. Add authorized origins and redirect URIs
# 7. Copy the Client ID and Client Secret below

# Frontend Google OAuth Configuration (Vite requires VITE_ prefix)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
VITE_REDIRECT_URI=http://localhost:5173/auth/google/callback

# Backend Google OAuth Configuration (DO NOT expose Client Secret to frontend)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-characters

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/supportdesk

# Server Configuration
PORT=3002
NODE_ENV=development

# CORS Configuration (for development)
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000

# Security Configuration
# CSRF Protection
CSRF_SECRET=your-csrf-secret-key-min-32-characters

# Session Configuration
SESSION_SECRET=your-session-secret-key-min-32-characters

# Rate Limiting (requests per minute)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# OAuth Security Settings
OAUTH_STATE_SECRET=your-oauth-state-secret-min-32-characters