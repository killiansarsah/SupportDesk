{
  "extends": ["stylelint-config-standard"],
  "rules": {
    "at-rule-no-unknown": [true, {
      "ignoreAtRules": ["tailwind", "apply", "variants", "responsive", "layer"]
    }],
    "property-no-unknown": [true, {
      "ignoreProperties": ["ring", "ring-color", "line-clamp"]
    }]
  }
}